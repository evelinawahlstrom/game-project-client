//import { USER_LOGIN } from "../actions/actions";


const user = (state = [], action) => {
    switch(action.type) {
       // case USER_LOGIN:
           // return action.payload
        default:
            return state 
    }
}

export default user
